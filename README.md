# Corp Portal  

## 📑 Содержание  
- [Описание задания](#описание-задания)    
- [Основные используемые технологии](#основные-используемые-технологии)  
- [Структура проекта](#структура-проекта)  
- [Запуск проекта](#запуск-проекта)
- [Описание проекта](#описание-проекта)
  
---

## Описание задания  

В качестве тестового проекта было предложено реализовать **картотеку граждан** или систему заявок **help desk**.  

Вместо того, чтобы выбирать одно направление, было решено объединить обе идеи и создать **корпоративный портал**, включающий следующие модули:  
- 📊 **Главная страница (Dashboards)** — метрики и графики, отображающие статистику по сотрудникам, документам и заявкам.  
- 📂 **Картотека сотрудников** — таблица с основными данными о сотрудниках и количеством заявок в help desk.  
- 🚹 **Карточка сотрудника** — детальная информация по разделам (табы), включая заявки сотрудника в help desk.  

---

## Основные используемые технологии  

### Зависимости  
- **React 19** — основной фреймворк для создания интерфейсов  
- **React Router 7** — маршрутизация приложения (Dashboard, Employees, Employee Card)  
- **Material UI (MUI)** — библиотека компонентов в стиле Material Design  
- **React Transition Group** — анимации при открытии/закрытии элементов (например, Sidebar)  
- **Recharts** — построение графиков с использованием готовых React компонентов  
- **Sass (SCSS Modules)** — препроцессор CSS с модульными стилями для изоляции  

### Dev-зависимости  
- **JSON Server** — фейковый REST API для имитации бэкенда. Изначально использовалась версия **1.0.0-beta.3** (последний релиз год назад), но были обнаружены баги с пагинацией. Было принято решение откатиться на стабильную версию **0.17.4**  
- **Concurrently** — запуск нескольких процессов (Vite dev server + JSON Server) одной командой  

### Данные  
- База данных была **сгенерирована с помощью нейросетей**  
- ⚠️ На достоверность и логичность данных обращать внимание не стоит

#### 1. Persons (Сотрудники)
Основная таблица с информацией о сотрудниках.

**Поля:**
- `id` — уникальный идентификатор
- `lastName` — фамилия
- `firstName` — имя
- `middleName` — отчество
- `birthDate` — дата рождения (формат: YYYY-MM-DD)
- `gender` — пол (`male` / `female`)
- `citizenship` — гражданство
- `maritalStatus` — семейное положение
- `address` — адрес проживания
- `phone` — контактный телефон
- `email` — электронная почта
- `consent` — согласие на обработку данных (boolean)
- `department` — подразделение (`IT`, `HR`, `Finance`, `Support`, `Marketing`)

**Связи:**
- Один ко многим с таблицами: Documents, FamilyMembers, Educations, Employments, Tickets

#### 2. Documents (Документы)
Информацию о документах сотрудников.

**Поля:**
- `id` — уникальный идентификатор
- `personId` — связь с сотрудником
- `docType` — тип документа (паспорт, загранпаспорт, водительское удостоверение)
- `series` — серия документа
- `number` — номер документа
- `issueDate` — дата выдачи (формат: YYYY-MM-DD)
- `issuedBy` — кем выдан
- `valid` — действителен ли документ (boolean)

#### 3. FamilyMembers (Члены семьи)
Информация о родственниках сотрудников.

**Поля:**
- `id` — уникальный идентификатор
- `personId` — связь с сотрудником
- `relativeId` — ID родственника (если он тоже сотрудник компании)
- `relation` — степень родства (мать, отец, сын, дочь, брат, сестра, супруг)
- `fullName` — ФИО родственника
- `birthDate` — дата рождения (формат: YYYY-MM-DD)
- `phone` — контактный телефон

**Особенности:**
- `relativeId` может быть null, если родственник не является сотрудником

#### 4. Educations (Образование)
История образования сотрудников.

**Поля:**
- `id` — уникальный идентификатор
- `personId` — связь с сотрудником
- `level` — уровень образования (среднее, СПО, высшее, магистратура)
- `institution` — учебное заведение
- `speciality` — специальность
- `startDate` — дата начала обучения (формат: YYYY-MM-DD)
- `endDate` — дата окончания (формат: YYYY-MM-DD)
- `honours` — окончил с отличием (boolean)

#### 5. Employments (Опыт работы)
История трудоустройства сотрудников.

**Поля:**
- `id` — уникальный идентификатор
- `personId` — связь с сотрудником
- `organization` — название организации
- `position` — должность
- `hireDate` — дата приёма на работу (формат: YYYY-MM-DD)
- `fireDate` — дата увольнения (формат: YYYY-MM-DD, null если текущее место)
- `current` — текущее место работы (boolean)
- `employmentType` — тип занятости (полная, частичная, фриланс)
- `experience` — количество лет опыта (number)

**Особенности:**
- При `current: true` поле `fireDate` игнорируется
- Для фрилансеров `organization` может быть пустым

#### 6. Tickets (Заявки в Help Desk)
Заявки в тех поддержку

**Поля:**
- `id` — уникальный идентификатор
- `personId` — связь с сотрудником (автор заявки)
- `title` — название заявки
- `category` — категория (`hardware`, `software`, `payments`, `access`)
- `priority` — приоритет (низкий, средний, высокий, срочный)
- `status` — статус (`open`, `in_progress`, `closed`, `resolved`)
- `createdAt` — дата создания (формат: YYYY-MM-DD)
- `updatedAt` — дата последнего обновления (формат: YYYY-MM-DD)
- `description` — описание проблемы

#### 7. Analytics (Аналитика)
Агрегированные данные для построения графиков.

**Поля:**
- `year` — год (например, 2020, 2021, 2022...)
- `employees` — количество сотрудников в этом году

**Особенности:**
- Используется для линейного графика на Dashboard
- Показывает динамику изменения численности персонала
---

## Структура проекта  

Проект организован по методологии **Feature-Sliced Design (FSD)** — архитектурный подход, который делит приложение на слои и слайсы для улучшения масштабируемости и поддерживаемости кода.

### Основные слои:
```text
src/
├── app/                    # Инициализация приложения
│   ├── providers/          # Провайдеры (роутер)
│   └── styles/             # Глобальные стили
│
├── pages/                  # Страницы приложения
│   ├── Dashboards.jsx      # Главная страница с метриками
│   ├── Employees.jsx       # Страница таблицы сотрудников
│   ├── EmployeeProfile.jsx # Страница карточки сотрудника
│   └── Root.jsx            # Корневой layout
│
├── widgets/                # Крупные самостоятельные блоки UI
│   ├── dashboard/          # Dashboard с графиками
│   ├── employee/           # Карточка сотрудника
│   ├── employees-table/    # Таблица сотрудников
│   └── sidebar/            # Боковое меню навигации
│
├── features/                 # Функциональные возможности
│   ├── employee-details/     # Работа с деталями сотрудника для карточки
│   ├── employees-filtering/  # Фильтрация таблицы
│   ├── employees-pagination/ # Пагинация таблицы
│   ├── employees-sorting/    # Сортировка таблицы
│   └── sidebar-toggle/       # Сворачивание/разворачивание sidebar
│
├── entities/               # Бизнес-сущности
│   ├── employee/           # Модель сотрудника + API
│   ├── document/           # Модель документа + API
│   ├── education/          # Модель образования + API
│   ├── employment/         # Модель опыта работы + API
│   ├── family-member/      # Модель члена семьи + API
│   ├── ticket/             # Модель заявки + API
│   └── analytics/          # Модель аналитики для графиков
│
└── shared/                 # Переиспользуемые компоненты и утилиты
    ├── api/                # Базовые классы для работы с API
    ├── ui/                 # UI-компоненты (Table, Modal, Skeleton и др.)
    └── hooks/              # Переиспользуемые хуки
```
---

### Ключевые решения:

- **API слой** — базовый класс `BaseApi` с методами `get`, `post`, `put`, `delete`, от которого наследуются специфичные API (employeeApi, documentApi и т.д.)
- **Пагинация, фильтрация, сортировка** — реализованы как отдельные features с кастомными хуками. Есть серверные фильтры (при помощи возможностей json server), а также сортировка/фильтрация непосредственно на странице. Делать сортировку и фильтрацию только на фронтенде было нерациональным, учитывая возможные объемы данных. 
- **Модальные окна** — карточка сотрудника открывается в Modal с использованием React Router
- **Формы** — кастомный хук `useFormUpdate` для управления состоянием форм с отслеживанием изменений

---

## Запуск проекта  

Установка зависимостей:  
```bash
npm install
```
Запуск dev-сервера Vite и JSON Server одной командой:
```bash
npm run go
```
После запуска приложение будет доступно по адресу http://localhost:5173, а API — на http://localhost:3001

---

## Описание проекта

### Общее представление

Приложение состоит из трех основных страниц:

- **Главная страница (Dashboard)** — аналитические данные и визуализация статистики:

  ![Главная страница](screenshots/page-1.png)

  <br>

- **Картотека сотрудников** — таблица с возможностью фильтрации, сортировки и пагинации:

  ![Картотека сотрудников](screenshots/page-2.png)

  <br>

- **Карточка сотрудника** — детальная информация с возможностью редактирования:

  ![Карточка сотрудника](screenshots/page-3.png)

---

### Sidebar (Боковая панель навигации)

Боковая панель служит для навигации между разделами приложения.

**Особенности:**
- Два пункта меню: «Главная» (Dashboard) и «Сотрудники» *(исходя из тестового задания)*
- Активный пункт подсвечивается синим цветом
- Возможность сворачивания/разворачивания панели
- Плавная анимация переходов между состояниями
- Всплывающие подсказки (tooltips) при наведении в свёрнутом состоянии

<br>

**Развёрнутое состояние:**

![Боковая панель развернута](screenshots/sidebar/sidebar-expanded.png)

<br>

**Свёрнутое состояние:**

![Боковая панель свернута](screenshots/sidebar/sidebar-collapsed.png)

<br>

**Пример tooltip:**

![Tooltip при наведении](screenshots/sidebar/tooltip.png)

---

### Таблица сотрудников

Основной интерфейс для работы с картотекой. Отображает ключевые данные: ФИО, дату рождения, пол, подразделение, телефон и количество заявок в Help Desk.

![Таблица сотрудников](screenshots/employees-table/employees-table.png)

<br>

#### Панель управления

Над таблицей расположена панель с инструментами фильтрации и сортировки:

![Панель управления](screenshots/employees-table/server-filters-and-sorting.png)

<br>

При нажатии на иконку раскрывается соответствующий инструмент:

![Развёрнутые фильтр и сортировка](screenshots/employees-table/expanded-filters-sort.png)

<br>

#### Серверная фильтрация и сортировка

**Принцип работы:**
- Фильтры применяются через выпадающие списки
- Сортировка активируется кнопками с индикацией направления (⬆️ по возрастанию, ⬇️ по убыванию)
- При повторном нажатии направление меняется, при третьем — сортировка отключается
- Изменения применяются мгновенно без дополнительного подтверждения
- Кнопка «Сбросить» появляется при наличии активных фильтров/сортировки

![Активные фильтр и сортировка](screenshots/employees-table/server-filters-sort-ex1.png)

<br>

После сворачивания панели активные инструменты подсвечиваются синим цветом:

![Свёрнутая панель с активными фильтрами](screenshots/employees-table/server-filters-sort-ex2.png)

<br>

При сбросе панель автоматически сворачивается:

![Сброс фильтров](screenshots/employees-table/server-filters-sort-ex3.png)

<br>

**Важное замечание о заявках:**

❗Так как по условию задания данных может быть на 100'000 строк, то запрос сразу всех данных и сортировка/фильтрация их на клиенте была бы нерациональна. Поэтому используется запрос на импровизируемый сервер - json server с его стандартыми возможностями для получения и селекции данных.

⚠️ Для заявок в helpdesk была создана отдельная таблица в json db, в связи с этим **сортировка и фильтрация по заявкам не может быть реализована** стандартными возможностями json server. Причина в том, что json-server не умеет работать с объединнеыми таблицами на уровне фильтров и сортировки. Но в инструментах в качестве заглушки была оставлено поле/кнопка для фильтрации/сортировки по заявкам. Данная фича может быть реализована посредством добавления кастомного middleware в json server, но из-за временных ограничений на задание не была реализована, так как не являлась приоритетной задачей. 


![Ограничение по заявкам](screenshots/employees-table/server-filters-sort-err.png)

<br>

#### Пагинация

Учитывая потенциально большой объём данных (около 100 000 записей), реализована пагинация:

**Возможности:**
- Навигация: `<` / `>` — предыдущая/следующая страница
- Быстрый переход: `<<` / `>>` — первая/последняя страница
- Индикатор текущих записей и количество всего записей через слэш (например, "1-10 / 150")
- Выбор количества строк на странице (10, 15, 20, 30)

![Пагинация](screenshots/employees-table/paginations.png)

<br>

#### Фильтрация и сортировка по текущей странице

Для демонстрации работы с данными на клиенте реализована дополнительная фильтрация/сортировка по загруженным строкам (на текущей странице).

**Сортировка:**
- При клике на заголовки «ФИО», «Дата рождения» или «Заявки» активируется сортировка
- Первый клик — сортировка по возрастанию (⬆️)
- Второй клик — по убыванию (⬇️)
- Третий клик — отключение сортировки

![Сортировка по странице](screenshots/employees-table/page-sort.png)

<br>

**Фильтрация:**
- При клике на заголовки «Пол» или «Подразделение» появляется всплывающий список фильтров
- После выбора фильтр применяется, а заголовок подсвечивается

![Фильтрация по странице](screenshots/employees-table/page-filters.png)

<br>

**Комбинирование:**

Можно одновременно использовать несколько типов фильтрации и сортировки:

![Комбинированная фильтрация](screenshots/employees-table/page-sort-filter-ex.png)

<br>

Серверная и клиентская фильтрация/сортировка работают совместно:

![Комбинация серверной и клиентской фильтрации](screenshots/employees-table/combinate-filters-sort.png)

---

### Карточка сотрудника

Открывается по клику на строку в таблице. Содержит **6 вкладок (табов)** с детальной информацией.

<br>

#### 1. Основные данные

Вкладка открывается по умолчанию:

![Основная информация](screenshots/employee-card/employee-card.png)

<br>

#### 2. Документы

![Документы сотрудника](screenshots/employee-card/employee-documents.png)

<br>

#### 3. Члены семьи

![Члены семьи](screenshots/employee-card/employee-family.png)

<br>

#### 4. Образование

![Образование](screenshots/employee-card/employee-educations.png)

<br>

#### 5. Опыт работы

*Примечание: для фрилансеров поле «Организация» может быть не указано*

![Опыт работы](screenshots/employee-card/employee-employment.png)

<br>

#### 6. Заявки (Help Desk)

При большом количестве данных появляется прокрутка:

**Верхняя часть:**

![Заявки — часть 1](screenshots/employee-card/employee-tickets-1.png)

<br>

**Нижняя часть:**

![Заявки — часть 2](screenshots/employee-card/employee-tickets-2.png)

<br>

#### Редактирование данных

Все поля карточки редактируемы. При внесении изменений появляются кнопки управления:

![Режим редактирования](screenshots/employee-card/card-edit.png)

<br>

**Поведение кнопок:**
- **«Сброс»** — возврат к исходному состоянию
- **«Сохранить изменения»** — отправка PUT-запроса на сервер

**Оптимизация запросов:**  
После сохранения повторный GET-запрос не выполняется — состояние обновляется локально. Это уменьшает нагрузку на сервер, но требует обновления страницы для синхронизации при одновременном редактировании несколькими пользователями. Такой подход обоснован предположением, что конкуренции при редактировании не будет (сотрудник редактирует свои данные или администратор работает единолично).

<br>

#### Параллельное редактирование элементов

Для вкладок с множественными записями (образование, опыт работы и т.д.) реализовано независимое редактирование каждого элемента:

![Редактирование нескольких элементов](screenshots/employee-card/card-edit-2.png)

<br>

При сохранении одного элемента остальные сохраняют своё состояние:

![Сохранение одного элемента](screenshots/employee-card/card-edit-3.png)

<br>

Кнопка «Сброс» работает только для конкретного элемента, не затрагивая другие:

![Изолированный сброс](screenshots/employee-card/card-edit-4.png)

<br>

**Техническая реализация:**  
Используется единое состояние с отслеживанием индексов изменённых элементов, что обеспечивает изоляцию без создания множества независимых состояний.

---

### Dashboard (Главная страница)

Страница визуализирует ключевые метрики и статистику через различные типы диаграмм. Все графики построены с использованием библиотеки **Recharts**.

<br>

#### Карточки метрик

![Карточки с основными показателями](screenshots/dashboard/dashboard-1.png)

<br>

#### Круговые диаграммы

Распределение по полу, департаментам, статусам заявок и валидности документов:

![Круговые диаграммы](screenshots/dashboard/dashboard-2.png)

<br>

#### Столбчатая диаграмма

Распределение сотрудников по годам рождения:

![Распределение по возрасту](screenshots/dashboard/dashboard-3.png)

<br>

#### Линейная диаграмма

Динамика изменения численности сотрудников по годам:

![Динамика роста](screenshots/dashboard/dashboard-4.png)

<br>

#### Общий вид страницы

![Dashboard полностью](screenshots/dashboard/dashboard.png)

---

